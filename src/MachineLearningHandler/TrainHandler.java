package MachineLearningHandler;

public class TrainHandler extends DecisionTreeNode {
    public TrainHandler(boolean decision, DecisionTreeNode leftChild, DecisionTreeNode rightChild) {
        super(decision, leftChild, rightChild);
    }


    //main handler but abstruct?
        //save the gamelog
        //train data

        // export to csv
        //return choice
}
