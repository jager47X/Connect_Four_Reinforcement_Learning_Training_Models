package MachineLearningHandler.Connect4Handler;

import MachineLearningHandler.DecisionTreeNode;
import MachineLearningHandler.TrainHandler;

public class AI extends TrainHandler {
    public AI(boolean decision, DecisionTreeNode leftChild, DecisionTreeNode rightChild) {
        super(decision, leftChild, rightChild);
    }
    //actually handle AI
}
